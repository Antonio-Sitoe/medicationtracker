import 'package:flutter/material.dart';

class PatientHistorysScreen extends StatelessWidget {
  const PatientHistorysScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Histórico Page'));
  }
}
